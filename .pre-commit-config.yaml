# Pre-commit hooks for code quality checks
# Install: pre-commit install
# Run manually: pre-commit run --all-files

repos:
  # Basic file checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v5.0.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
        args: ['--maxkb=1000']
      - id: check-merge-conflict
      - id: check-toml
      - id: debug-statements
      - id: mixed-line-ending

  # Ruff - linting and formatting
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.4
    hooks:
      # Run the linter
      - id: ruff
        args: [--fix]
      # Run the formatter
      - id: ruff-format

  # MyPy - type checking (optional, non-blocking)
  # Note: Currently has pre-existing errors. Uncomment to enable blocking type checks.
  # - repo: https://github.com/pre-commit/mirrors-mypy
  #   rev: v1.13.0
  #   hooks:
  #     - id: mypy
  #       additional_dependencies:
  #         - sqlalchemy[mypy]
  #         - types-redis
  #         - types-pillow
  #         - types-bleach
  #         - types-pyyaml
  #       args: [--config-file=pyproject.toml]
  #       files: ^src/

  # Optional: Run a subset of fast tests
  # Uncomment if you want to run tests on commit (can be slow)
  # - repo: local
  #   hooks:
  #     - id: pytest-quick
  #       name: pytest-quick
  #       entry: pytest -m "not slow" --maxfail=1 -x
  #       language: system
  #       pass_filenames: false
  #       always_run: true
